Place the 3D model file for `traveling_microscope_3d.html` here.

Expected filename:

`traveling_microscope.glb`

If you use a different filename, update `MODEL_URL` inside `sims/traveling_microscope_3d.html`.
